# --- Second Brain — Personal Knowledge Agent (Agentic RAG) ---
# Web UI
streamlit>=1.36

# LLM (Google Gemini via GCP API key) — current SDK
google-genai>=1.0.0

# Local embeddings + reranking (fully offline)
sentence-transformers>=3.0.0

# Vector store (documents + semantic memory)
chromadb>=0.5.5

# Multi-format document processing
PyPDF2>=3.0.0
pypdf>=3.17.0
python-docx>=1.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
markdown>=3.5.0

# Utilities
python-dotenv>=1.0.1
pandas>=2.0.0
python-dateutil>=2.8.2
